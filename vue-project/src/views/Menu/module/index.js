import actions from './actions';
import mutations from './mutations';

const menu = {
   state:{
      categories:[],
      foods:[],
      count: 0
   },
   actions,
   mutations
};

export default menu;